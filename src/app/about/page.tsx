import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Sobre',
  description: 'Essa é a página sobre do blog',
};

export default function AboutPage() {
  return <h1>Hello World!</h1>;
}
